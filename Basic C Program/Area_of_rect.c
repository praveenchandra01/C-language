#include<stdio.h>
#include<conio.h>
int main()
{
  int l,b,a;
  printf("Enter the length and breath of Rectangle :");
  scanf("%d%d",&l,&b);
  a=l*b;
  printf("Area of rectangle is %d",a);
  return 0;
  getch();
}
